; sharp (transitive dep of wrangler → miniflare) has a broken source-build
; fallback on newer Node versions: its install script wrongly rejects the
; prebuilt binary and then fails. The prebuilts install fine as plain
; optional deps, and nothing in the deploy path needs install scripts.
ignore-scripts=true
